// import { IUser } from "../models/User.model";

// declare global {
//   namespace Express {
//     interface Request {
//       user?: IUser & { _id: string }; // Ensure `_id` is explicitly defined
//     }
//   }
// }
